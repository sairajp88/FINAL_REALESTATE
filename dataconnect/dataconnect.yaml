specVersion: "v1"
serviceId: "finalrlstproject"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "finalrlstproject-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
